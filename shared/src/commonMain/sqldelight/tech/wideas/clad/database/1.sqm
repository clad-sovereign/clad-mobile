-- Migration from schema version 1 to 2
-- Adds AppSettings table for storing app-wide settings like active account ID

CREATE TABLE AppSettings (
    key TEXT NOT NULL PRIMARY KEY,
    value TEXT NOT NULL
);
